import styled from "styled-components/macro";

export const ScoreStyles = styled.div`
  color: white;
  .high-scores {
    position: fixed;
    inset: 0;
    .content {
      margin-top: 128px;
    }
  }
`;
